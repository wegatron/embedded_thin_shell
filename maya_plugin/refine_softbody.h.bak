#ifndef REFINE_SOFTBODY_H
#define REFINE_SOFTBODY_H
#include <vector>
#include <maya/MFnDagNode.h>

#define LOG_FILE "c:\\maya_refine_softbody_log.txt"

#define JUDGE_LOG(ostream, msg, status) do {      \
  if(status!=MStatusCode::kSuccess) { \
      ostream << msg << endl;
  } \
}while(0)

Mstatus DuplicateSelected (std::vector<MObject*> &duplicated_nodes_vec);
Mstatus RefineMesh (std::vector<MFnDagNode*> &duplicated_nodes_vec);

Mstatus RefineSelected (Mtime start, Mtime end);

#endif /* REFINE_SOFTBODY_H */
